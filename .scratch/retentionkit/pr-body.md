Partner apps duplicate notification, widget, uninstall-feedback and rating workflows, making scheduling, eligibility and destination handling inconsistent. RetentionKit centralizes those mechanics behind a localized feature catalogue and a shared runtime, with five selective foundation/feature artifacts plus an umbrella facade.

The common notification profile supplies eight campaigns, durable budgets/priority/rotation, explicit permission/subscriber/setup gates and shared Firebase legacy-key mapping. Every external body/action/widget/shortcut and feedback rescue enters the real Splash flow, uses existing OnboardKit entry interstitial policy, waits for Main resume, then consumes only the selected destination. A newer invalid/ordinary Intent cannot revive an older pending entry. Accepted notification routes survive a longer onboarding journey without weakening expired-send or explicit core-expiry checks.

The generic EN/VI example implements Notes, Saved items, Text tools and Guide with preserved legacy data and stable business-success IDs. Existing Ads supplies medium native_noti/native_widget/native_uninstall. The Uninstall shortcut has localized text and independent icon branding; optional reasons, Keep and feature rescue use the SDK controller, and Continue opens Android uninstall confirmation with an App Info fallback. Play review remains outcome-unknown and independent of feedback. Optional Onboard/Ads, authoritative Billing, Trackkit and shared Firebase integration preserve existing owners and callbacks.

Validation at frozen source69d0f71 (production/config/tests identical to device86048d6):

- 279 fresh JVM/Robolectric cases across nine affected modules in one `--rerun-tasks` invocation,0 failures/errors/skips; unrelated legacy suites are not relabelled as fresh.
- 17/17 actual unassisted Pixel5/API34 cases, exact Activity/token/consumption checks and unchanged installed APK hashes.
- Genuine first-install API36 defaultrevision0: permission deny/allow, visible ONBOARD notification, same accepted token through >5min setup to Notes, actual native_noti loaded/impression. Only isolated emulator OS time advanced25h before the journey to reach install grace.
- Six fresh local QA publications and12/12 project/POM-only Maven release R8/runtime graph/manifest/composition checks at `retentionkit-qa-20260907-69d0f71`; full example release R8 passed. Only Crashlytics mapping upload excluded; repository debug signing retained.
- Actual minified release API36 smoke passed update-install with data retained, reboot then explicit launcher to Main, Uninstall shortcut to standard survey and force-stop/reopen restoring seven owned alarms; no BOOT-only wake claim.
- Independent corrected Standards and Spec reviews report no remaining actionable findings. RED/GREEN and exact evidence are linked in `.scratch/retentionkit/correction-acceptance.md` and `verification.md`.

Calendar QA validates saved-envelope/receiver behavior, not OS wake timing. Play review/card/rating and uninstall completion are not inferred; the unpublished example's real Store handoff returns Item not found. Final physical QA/restoration and task-resource cleanup stay root-owned. These are locally published QA artifacts, not a remote release; existing5.1.1 does not include RetentionKit. No release tag or PR merge is requested. PR creation/readiness itself still requires an account with repository write permission; this prepared body does not claim a PR exists.
