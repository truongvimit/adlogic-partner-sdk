# Working RetentionKit example

The original Ad Showcase remains the launcher destination. Its **Everyday tools / Công cụ hằng ngày** button opens four genuine offline features: twelve EN↔VI travel phrase translations, a persistent saved phrase list, whitespace normalization with word/Unicode character counts, and a bundled document with reading-time calculation. These are small complete utilities, not a general translation or document SDK. The selected app language supplies one shared feature catalogue for screens, notifications, widget tiles, feedback suggestions and shortcuts.

## Partner integration

`GlobalApp` installs the existing Tracker/Firebase, ads/billing/paywall and OnboardKit first, then calls `RetentionExample.install` once. The Retention facade uses the shared Firebase config client and Trackkit event sink. The same Onboard bridge supplies routing, UI availability and adapters. The standard process starts with UNKNOWN entitlement; only the authoritative Billing adapter may supply verified premium/free status. Ordinary release uses all module defaults, including setup grace, campaign caps/cooldowns and review threshold 5 / cooldown 10 days / max 3 attempts. No startup network request is awaited by Retention installation.

The host gate allows tools, Settings and SDK feedback, excluding the Ad Showcase's independent consent/update/dialog UI. The new utility screen is excluded from existing app-resume ads. The Onboard bridge owns actual suite ad-click observation; the app does not duplicate it through analytics or per-placement callbacks.

Splash captures typed entries before starting async setup and retains the rewritten envelope. Ready users go to the utility Activity; unfinished users keep a durable pending entry through normal OnboardKit. A terminal Completed/Skipped outcome forwards to the resumed destination; Aborted discards only entries attached to that unfinished setup. `onNewIntent` calls `setIntent` and capture, never restarts `onCreateSafe`. The utility Activity dispatches after resume/decor-post, retries temporarily blocked entries for up to five seconds, and leaves still-blocked entries durable for a later resume. Facade dispatch validates destinations and claims once immediately before mapping to one of the four real feature views. A crash between claim and navigation remains an at-most-once limitation.

Route selection belongs to the latest explicitly captured or restored token. After that token is consumed, an older entry in the durable ledger is not automatically selected. New Intent callbacks coalesce their pending dispatch and receive a fresh bounded retry allowance; resume/recreation does not drain unrelated older routes.

Each completed operation is persisted together with a stable ID and an app outbox entry. SDK submission uses that ID. An owned per-runtime subscriber acknowledges the matching outbox operation only after core state persistence and signal dispatch; a queued Boolean return never acknowledges it. State/acknowledgement failure leaves it pending. Replaying after a crash uses the same ID for review deduplication. This dispatch acknowledgement does not prove every asynchronous subscriber completed, a review was shown, or a rating occurred. Opening/recreating a screen and no-op saves emit no business success. Raw user text stays in local app preferences, outside telemetry.

Settings Feedback and the legacy uninstall compatibility target now open the SDK's optional standard feedback flow. Settings Rate opens the store directly and does not set a fabricated `isRate`. Migration removes only the known `ACTION_OPEN_UNINSTALL` dynamic shortcut. Other shortcut IDs remain untouched; pinned legacy Splash entries are translated to standard feedback.

## Debug QA and evidence boundaries

Debug contains an explicitly activated **isolated engine QA** panel. Standard debug startup still uses production defaults and system clock. Activating a fixture unregisters the ordinary runtime, clears only `retention_example_engine_qa_v1`, and installs the real facade/modules with an explicit synthetic entitlement/setup state and controllable wall clock. The real Android permission/channel gates, notification builder/notify, alarm receiver, PendingIntents, widget launcher and Play transport remain intact. Calendar QA reads an actual saved `schedule:` envelope, advances the fixture clock only when its due time is in the future, and invokes the real receiver. An already-due envelope keeps the current fixture time; an expired envelope stays expired. This is engine-driven handling evidence; it does not prove AlarmManager wake, reboot, force-stop, Doze, OEM timing or screen wake. Delayed onboarding/ad-return tests use real process Home transitions and the production three-second callback; the QA ad click is explicitly synthetic.

Calendar regression tests pin fixture initialization before setup/scheduling. The two DAILY device cases use 08:56 local on the following day: the saved 08:00 envelope is still inside its one-hour TTL in fixture time, while the real AlarmManager cannot race the manual receiver delivery during the test. Assertions verify these time relationships and include current time, setup time and recent gate events on timeout. This guards the physical-device finding where rewinding to 08:00 made a valid 08:56 setup appear to be in the future and incorrectly exercised the cooldown gate instead of posting or checking the channel. Production eligibility and scheduling are unchanged.

The panel can select completed or active unfinished setup, subscriber/free state, foreground reminder/pinned, calendar delivery and ad-return/onboarding. Its status shows the active profile, user state, config revision, foreground, actual tagged OS notification count and latest truthful SDK event. Notification settings use the existing scoped external transition and reconcile on return. Buttons report request/submission eligibility, never pin/review/uninstall success. **Restore production defaults** tears down the fixture and reinstalls the normal store, system clock and suite bridges. Process restart also starts standard mode. No release QA class implements fixture selection, synthetic clock, state reset or a test receiver; release has only harmless UI no-ops.

## Validation

Accepted on 2026-09-07 at delivery source `2ff967a909cf043bf6e66b3424258a78efe67947`. Build and Android execution used clean private head `97cc904486b8ef2219dcf56dabe2130a4ba5ef0e`, whose whole Git tree matched that delivery source before and after both runs. The following evidence is stored outside the worktree; links are local to the delivery environment.

| Check | Observed result | Evidence |
| --- | --- | --- |
| Fresh app unit/Robolectric execution | 17/17 passed; zero failures, errors or skips | [Actual JUnit summary](/Users/Shared/Panacea/Documents/SDKOptimize/evidence/retentionkit-final-app-20260907-2ff967a/tests.json) |
| Full product unit scope | 549/549: 532 unchanged non-app cases from `bf68f1e` plus the 17 fresh app cases; these are two recorded invocations | [Combined scope and XML provenance](/Users/Shared/Panacea/Documents/SDKOptimize/evidence/retentionkit-final-app-20260907-2ff967a/combined-product-tests.json) |
| Debug APK, instrumentation APK and minified release APK | All assembled; R8 release passed. Only Crashlytics mapping upload was excluded | [Exact command, source and log hashes](/Users/Shared/Panacea/Documents/SDKOptimize/evidence/retentionkit-final-app-20260907-2ff967a/run.json) |
| Actual Android instrumentation | 15/15 passed on the API36 emulator; zero failures, errors or skips | [JUnit verification](/Users/Shared/Panacea/Documents/SDKOptimize/retentionkit-device/example-api36-run-6-final-verification.json), [execution and source record](/Users/Shared/Panacea/Documents/SDKOptimize/retentionkit-device/example-api36-run-6-final/run.json) |

Preserved outputs: [debug APK](/Users/Shared/Panacea/Documents/SDKOptimize/evidence/retentionkit-final-app-20260907-2ff967a/artifacts/app-debug/ITG_Base_Project_v1.0.0_v100_09.07.2026-debug.apk), [instrumentation APK](/Users/Shared/Panacea/Documents/SDKOptimize/evidence/retentionkit-final-app-20260907-2ff967a/artifacts/app-androidTest/ITG_Base_Project_v1.0.0_v100_09.07.2026-debug-androidTest.apk), [release APK](/Users/Shared/Panacea/Documents/SDKOptimize/evidence/retentionkit-final-app-20260907-2ff967a/artifacts/app-release/ITG_Base_Project_v1.0.0_v100_09.07.2026-release.apk). The [artifact manifest](/Users/Shared/Panacea/Documents/SDKOptimize/evidence/retentionkit-final-app-20260907-2ff967a/artifacts.json) records their SHA256 values and the preserved R8 mapping. Release APK SHA256: `91a66bcd4f359d4e4ba02a754dea9650f2f92847a38d6fbf30d93f4d2f9c591d`.

App unit tests cover real computation, input bounds, durable result/outbox restoration, duplicate operations, no-op saves, failed persistence, concurrent queued-dispatch replay and actual Activity routing. The 15 Android cases exercise the real engine and Android transports: all seven notification campaigns; four distinct pinned actions reaching usable feature screens; foreground/config/subscriber suppression; pending setup → resumed destination and replay rejection; outbox/review deduplication; a real blocked channel; real Play request/terminal handling; and optional-reason feedback Keep/rescue/App Info + Back. Pinned actions require a newly delivered ONCE token with matching source/destination, its durable consumed receipt, visible feature title and unchanged Activity/task ownership.

The blocked-channel test creates a unique channel at actual Android IMPORTANCE_NONE, uses the public channelIds option and a genuine saved DAILY alarm, and requires skipped(channel_blocked), no active post and unchanged NONE importance. Teardown deletes only its owned channel. The Play smoke explicitly accelerates eligibility to threshold1 and five-second timeouts, completes a real phrase translation, and requires requested → failed/timeout/outcome-unknown with the core UI lease released. It does not require or infer a card, rating or review acceptance. Feedback tests use actual SDK buttons and Android App Info: no reason is required; rescue reaches a consumed, functional text tool before any Home/restart; Back restores UI ownership.

The device runner must grant POST_NOTIFICATIONS before this suite; absent permission fails the precondition rather than becoming a skipped pass. Run on a disposable installation because instrumentation creates and clears Retention notifications. Cleanup touches only SDK-tagged notifications and its test-owned channel, restores the normal runtime and restores ActivityScenario's launch identity only for harness teardown. No production transport is replaced.

Commands used for the final affected app validation:

```sh
./gradlew :app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest :app:assembleRelease -x :app:uploadCrashlyticsMappingFileRelease --no-daemon --console=plain --max-workers=2 --stacktrace --init-script /Users/Shared/Panacea/Documents/SDKOptimize/evidence/retentionkit-final-app-20260907-2ff967a/force-product-tests.gradle
ANDROID_SERIAL=emulator-5554 ./gradlew :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.itg.template.retention.RetentionExampleEngineTest -Pandroid.injected.androidTest.leaveApksInstalledAfterRun=true --console=plain --max-workers=2
```

The preserved external init script forces actual unit-test execution instead of reusing test-result cache. Connected execution belonged to the root device owner. Firebase configuration remains ignored and supplied by the existing project environment. Existing ByteDance/Facebook R8 warnings were preserved in the successful build log.

## Regression history and remaining limits

The first API36 run passed 9/11. Two test-harness errors were corrected: ActivityScenario lost its launch identity after required `setIntent` changes, and a review-counter test had explicitly disabled review. Assertions now preserve Activity/task identity, restore only the harness Intent at teardown, use the selected app locale and enable the dedupe counter with threshold1000/zero Play attempts. Subsequent 13- and 15-case checkpoints passed as recorded in the root ledger.

A later 14/15 run at `bf68f1e` exposed a real burst-routing defect as well as a weak first-action acknowledgement: multiple `onNewIntent` callbacks could consume the newest route and then select an older ledger entry. A minimized test of the actual utility Activity reproduced the wrong final feature before correction. Commit `a4d3c312` removed automatic ledger fallback, coalesced pending dispatch and reset retries per newly captured token. Three passing regressions cover two deliveries before dispatch, an eight-Intent burst with resume/recreation, and a new token after exhausted retries. The strengthened pinned device test then passed in the final 15/15 run above. The [prior failed device result](/Users/Shared/Panacea/Documents/SDKOptimize/retentionkit-device/example-api36-run-5-review-fix-failure-verification.json) remains preserved; it is not counted as a pass.

Separate root-owned API36 manual evidence at the earlier `1d06439` checkpoint used normal suite adapters and defaults: confirmed launcher pin; widget TextTools → real consent/language/onboarding/PayKit → word normalization; VI widget-label refresh and warm Document routing; permission denied → no post → grant → real silent posts; and manual Rate → correct package's Store page → Back. The unpublished example's Store page can report Item not found. These checks are distinct from the isolated QA suite.

Actual emulator force-stop removed seven alarms and user relaunch restored seven; timezone changes recomputed/restored them; guest OS reboot restored seven before manual launch after initialization settled. See [force-stop](/Users/Shared/Panacea/Documents/SDKOptimize/retentionkit-device/example-api36-manual/force-stop-verdict.json), [timezone](/Users/Shared/Panacea/Documents/SDKOptimize/retentionkit-device/example-api36-manual/timezone-verdict.json) and [reboot](/Users/Shared/Panacea/Documents/SDKOptimize/retentionkit-device/example-api36-manual/reboot-verdict.json) records. These prove recovery observed on that emulator, not alarm-delivery timing, a boot SLA, Doze wake or OEM behavior.

The attached physical Pixel5 API34 remained PIN-locked for Retention acceptance; no full physical-device pass is claimed. Play controls card eligibility/quota and reveals no submitted-rating result. Launcher acceptance, notification delivery timing and OEM lifecycle behavior remain platform-controlled. Ticket06's implemented app integration is accepted; broader publication/composition evidence and these limits remain recorded separately in the root verification ledger.
