<?xml version="1.0" encoding="utf-8"?>
<!-- Question option card: image with rounded corners, tick when selected, label below. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="4dp">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/ob_option_image"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:background="@drawable/ob_bg_language_row"
            android:importantForAccessibility="no"
            android:scaleType="centerCrop" />

        <View
            android:id="@+id/ob_option_overlay"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:background="@drawable/ob_bg_language_row"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/ob_option_tick"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="end|top"
            android:layout_margin="8dp"
            android:importantForAccessibility="no"
            android:src="@drawable/ob_ic_check"
            android:visibility="gone" />
    </FrameLayout>

    <TextView
        android:id="@+id/ob_option_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:ellipsize="end"
        android:fontFamily="@font/ob_poppins_medium"
        android:gravity="center"
        android:maxLines="1"
        android:textColor="@color/ob_text_primary"
        android:textSize="14sp"
        tools:text="Romance" />
</LinearLayout>
