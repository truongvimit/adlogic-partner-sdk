<?xml version="1.0" encoding="utf-8"?><!--
  No permissions and no default Consent Mode meta-data on purpose: declaring
  google_analytics_default_allow_* here would silently override the host app's own defaults
  through manifest merging. FirebaseSink documents where those belong.
-->
<manifest />
