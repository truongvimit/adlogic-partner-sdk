# Standard entry integration contract

Correction ticket10 SDK contract; affected SDK tests and release AARs are verified below. Full example/consumer/device correction acceptance is tracked separately. Reuse the existing entry ledger, and select only the envelope that arrived or was restored with the current Activity.

- `OnboardRetentionBridge(splashActivity, hostCanPresent = { true }, entryAdPolicy = RetentionEntryAdPolicy.STANDARD, mainActivity = MainActivity::class.java)`: standard router uses actual `SplashEntry.intent` (`NEW_TASK | CLEAR_TASK`). Default entry interstitial policy stays in OnboardKit. `WITHOUT_SPLASH_ADS` is explicit opt-in. Original one-argument and trailing-lambda constructors remain source compatible.
- Notification sources map to NOTIFICATION; WIDGET/SHORTCUT to WIDGET; feedback destination or FEEDBACK source to UNINSTALL. All rescue taps reopen Splash and preserve the FEEDBACK source.
- `bridge.mainIntent(context, mainActivity, outcome): Intent?` signals the existing terminal state and copies only the outcome passthrough. Completed/legitimate Skipped route Main; Aborted returns null. The existing listener retains its other responsibilities.
- `kit.mainHandoff(activity, savedInstanceState, featureRouter: RetentionRouter): RetentionMainHandoff`: bind during Main.onCreate, before onStart. The helper observes resumed/pause/destroy and window-focus return, retries readiness at most 10 seconds per resume or focus gain and owns bounded resume-suppression handles. A dialog that outlives the retry window can therefore resume the selected handoff on focus return without a new Activity resume or host callback. Polling stops between readiness events; close removes the focus listener even when Android replaces the pre-attachment decor observer. Forward `onNewIntent(intent)` and `onSaveInstanceState(out)`. `close()` is idempotent and automatic on destruction; `hasPendingEntry` reads only the selected pending token.
- Main forwarding retains the exact materialized envelope and **does not consume** feature entries. The final router must return an explicit in-package feature Activity, or null to leave it pending. It may validate known retired aliases without rewriting the token/destination. Final feature capture/dispatch owns consume and uses ENTRY at both readiness rechecks, independently of automatic PROMPT eligibility. Failed routers remain retryable; an already-forwarded selection survives recreation. No arbitrary old backlog entry is selected.
- Configure `mainActivity` on the bridge. Main never passes automatic PROMPT eligibility. Explicit ENTRY requires the host predicate, actual resumed/foreground Activity and all shared onboarding/ad/external-transition gates. Main continuation validates its exact selected pending ONCE envelope in the helper; an explicit menu start via `feedback.openViaEntry()` needs no pre-existing token. The host predicate expresses dialog/focus readiness and should not duplicate the helper’s pending-token check, which would block such menu starts. The helper reserves resume suppression before Main.onStart. It does not create another interstitial.
- A non-Onboard host can use vendor-free `RetentionSplashRouter(splashActivity: Class<out Activity>)` from core, then forward the rewritten envelope through its own Splash/setup and Main. The low-level `RetentionRouter` remains available for deliberately custom integrations.

## Feedback

`feedback.openViaEntry(): FeedbackShowResult` is the standard menu/button entry. It routes through configured Splash; only Main-ready `handleEntry(materializedEntry)` launches the SDK session Activity. `show()` remains an advanced direct PROMPT API. A blocked explicit entry stays pending; consumption occurs only immediately before the final survey launch. Accepted platform start followed by failure/process death remains at-most-once, not guaranteed exactly-once navigation.

`FeedbackOptions` appends `systemAction = UNINSTALL_CONFIRMATION`, `appManagementFallback = true`, and `nativeContent: FeedbackNativeContent? = null`. Default Continue uses Android `ACTION_UNINSTALL_PACKAGE` and the merged `REQUEST_DELETE_PACKAGES` permission. Android/user owns confirmation. Failed launch can fall back to App Info after rechecking the same scope/config/session. `APP_MANAGEMENT` selects App Info explicitly; existing `controller.continueToAppManagement()` remains. No action/event claims successful uninstall.

The existing `FeedbackUiFactory.create(activity, controller, content): View` stays source compatible. `RetentionFeedbackActivity` is a `LifecycleOwner`. `FeedbackNativeContent.bind(activity, lifecycleOwner, container: ViewGroup): AutoCloseable` is vendor-free. Default UI creates a slot; a custom factory calls `controller.bindNative(itsContainer)` once per slot. Controller owns native bindings until destroy, including recreation; `controller.lifecycleOwner` exposes lifecycle for other custom content. Use controller actions `selectReason`, `keep`, `tryFeature`, `continueToSystem`, and optional explicit `continueToAppManagement`; never replace the state machine. Reasons remain optional. The custom Activity retains system/IME insets and durable session restoration.

Construct a **new bridge per new runtime install** (including tests calling uninstallForTests); shutdown cancels its subscriptions. Do not reuse a detached singleton adapter. Repeated install without teardown still returns the original facade.

## Verified SDK scope

At the initial correction checkpoint `ef94857ad869bd7345c10f24e66dde318cf08396`, one actual Gradle invocation passed **162/162** tests, zero failures/errors/skips: core49, feedback28, facade28 (including composite legacy config3), widgets37 and review20. The same invocation assembled all five release AARs in1m49. [Immutable result, XML and AAR hashes](/Users/Shared/Panacea/Documents/SDKOptimize/retentionkit-correction/evidence/sdk-full-ef94857/result.json).

Four defects were first reproduced by the same targeted15 cases at9984e98 (4fail), then passed15/15 at ef94857: nested onNewIntent from resource close/final host gate and both final dispatch purpose checks. [RED](/Users/Shared/Panacea/Documents/SDKOptimize/retentionkit-correction/evidence/entry-red-9984e98/result.json), [GREEN](/Users/Shared/Panacea/Documents/SDKOptimize/retentionkit-correction/evidence/entry-green-ef94857/result.json). Main capture uses a delivery generation; selection is rechecked after arbitrary host callbacks; successful forwarding releases only its own token's resource. Targeted cases are included in the162, not additional product tests.

Coverage includes real lifecycle pause/recreation, materialized reusable identity, exact/rapid/duplicate selection, failed router retry, owned resource cleanup, standard/explicit-no-ad Splash flags, Main prompt separation, optional reasons, native binding lifecycle, default confirmation/explicit App Info/fallback rejection, and invitation-only disable preserving existing widgets/direct pin. These are JVM/Robolectric and AAR results, not evidence of OS dialog visibility, actual ads/native fill, OEM behavior, consumer R8, Play rating or successful uninstall. Those remain integration/device acceptance work. No SDK artifact was remotely published.

The focus follow-up at `7c3470d` passed **30/30 fresh facade tests and the facade release AAR** in12s. Two real Robolectric window-focus regressions first reproduced the missing continuation at `e9358f3` (1 failure), then both passed: a selected entry resumes after >15 seconds unfocused without any pause/resume, polling stops after its budget, and closing before focus return remains inert. Listener ownership stays inside the SDK. [RED](/Users/Shared/Panacea/Documents/SDKOptimize/retentionkit-correction/evidence/focus-red-e9358f3/result.json), [GREEN](/Users/Shared/Panacea/Documents/SDKOptimize/retentionkit-correction/evidence/focus-green-7c3470d/result.json), [full facade result](/Users/Shared/Panacea/Documents/SDKOptimize/retentionkit-correction/evidence/focus-full-7c3470d/result.json). Core49, feedback28, widgets37 and review20 retain the earlier134 results after verifying identical complete module trees and XML hashes. The resulting164 tests are scoped provenance across invocations, not one new run; the two focus cases are already included in facade30. App/device/consumer verification of this follow-up remains separate.
